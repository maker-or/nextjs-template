# The CLI writes these values to .env.local.
NEXT_PUBLIC_OPENCMS_PROJECT_ID=
# Set this to the OpenCMS API origin. The CLI fills this for generated apps.
OPENCMS_API_URL=
OPENCMS_ENVIRONMENT=development
